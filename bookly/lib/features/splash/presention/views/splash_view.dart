import 'package:bookly/features/splash/presention/views/widgets/splash_view_body.dart';
import 'package:flutter/material.dart';

class splash_view extends StatelessWidget {
  const splash_view({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: splash_View_Body());
  }
}
